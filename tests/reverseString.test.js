import reverseString from '../JS/reverseString.js'

test('Reverse string', () => {
  expect(reverseString('aimen')).toBe('nemia');
})

test('Reverse string', () => {
  expect(reverseString('mohamed')).toBe('demahom');
})

test('Reverse string', () => {
  expect(reverseString('khraa')).toBe('aarhk');
})

